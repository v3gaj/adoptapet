<% if @errors.any? %>

	$('#render-panels').html("<%= j (render :template => 'pets/gallery.html') %>");

	masonryGallery();

	lazyMasonry();

	prettyPhot();

  $('#photo_errors').html("<%= j (render 'errors') %>");

<% else %>

	$('#render-panels').html("<%= j (render :template => 'pets/gallery.html') %>");

	masonryGallery();

	lazyMasonry();

	prettyPhot();

<% end %>